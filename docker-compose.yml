services:
  claude-observe:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      SERVER_PORT: "4001"
      DB_PATH: /data/observe.db
      ENABLE_WEBSOCKET: "true"
      SERVER_LOG_LEVEL: "debug"
    ports:
      - "${PORT:-4001}:4001"
    volumes:
      - ./data:/data
